<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical" >

    <!-- 发送时间 -->
    <TextView android:id="@+id/tv_chattime" android:layout_width="wrap_content"
        android:layout_height="wrap_content" android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" android:layout_marginTop="14dp"
        android:padding="3dp"
        android:text="2020年9月13日10:55:58" android:textColor="#FFFFFF"
        android:textSize="12sp" />

    <!-- 文字内容 -->
    <TextView android:id="@+id/tv_chatneirong" android:layout_width="wrap_content"
        android:layout_height="wrap_content" android:layout_alignTop="@+id/iv_chattou"
        android:layout_toLeftOf="@+id/iv_chattou"
        android:gravity="center" android:padding="10dp"
        android:text="qw速度速度多少e" android:layout_marginTop="10dp"
        android:textSize="16sp"  android:maxLines="10"
        android:maxEms="10"
        />

    <!-- 头像 -->
    <ImageView android:id="@+id/iv_chattou" android:layout_width="40dp"
        android:layout_height="40dp" android:layout_alignParentRight="true"
        android:layout_below="@+id/tv_chattime" android:layout_marginRight="14dp"
         />



</RelativeLayout>